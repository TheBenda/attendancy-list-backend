---
apiVersion: "gateway.networking.k8s.io/v1"
kind: "HTTPRoute"
metadata:
  name: "localtest-gateway-auth-localtest-me-route"
spec:
  parentRefs:
    - name: "localtest-gateway"
  hostnames:
    - "auth.localtest.me"
  rules:
    - matches:
        - path:
            type: "PathPrefix"
            value: "/"
      backendRefs:
        - name: "zitadel-service"
          port: 8080
