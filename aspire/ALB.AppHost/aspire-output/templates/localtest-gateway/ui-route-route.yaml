---
apiVersion: "gateway.networking.k8s.io/v1"
kind: "HTTPRoute"
metadata:
  name: "localtest-gateway-ui-route-route"
spec:
  parentRefs:
    - name: "localtest-gateway"
  hostnames:
    - "ui-route"
  rules:
    - matches:
        - path:
            type: "PathPrefix"
            value: "/"
      backendRefs:
        - name: "vite-app-service"
          port: 5000
