---
apiVersion: "v1"
kind: "ConfigMap"
metadata:
  name: "vault-config"
  labels:
    app.kubernetes.io/name: "{{ .Chart.Name }}"
    app.kubernetes.io/component: "vault"
    app.kubernetes.io/instance: "{{ .Release.Name }}"
data:
  VAULT_ADDR: "{{ .Values.config.vault.VAULT_ADDR }}"
